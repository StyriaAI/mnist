# model server uri in format host:port
model_server_uri = "localhost:9000"

# name of the model
model_name = "mnist"

# shape of the image expected by the model
target_image_shape = (28, 28)

# port to use in development
dev_port = 5000
